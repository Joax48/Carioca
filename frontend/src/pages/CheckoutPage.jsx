// CheckoutPage — placeholder

export function CheckoutPage() {
  return (
    <div style={{ minHeight: '100vh', display: 'flex', flexDirection: 'column',
      alignItems: 'center', justifyContent: 'center', gap: 16 }}>
      <h1 style={{ fontFamily: 'var(--font-display)', fontSize: 36, fontWeight: 300,
        fontStyle: 'italic', color: 'var(--color-espresso)' }}>Checkout</h1>
      <p style={{ fontFamily: 'var(--font-body)', color: 'var(--color-espresso-60)' }}>
        Próximamente.
      </p>
    </div>
  );
}
